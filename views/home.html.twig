<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <form class="" action="/result" method="post">
            <label for="">Enter a word:</label>
            <input type="text" name="input_word" value="" required>
            <button type="submit" name="button">PLAY</button>
        </form>
        {% if score%}
        <h2> {{score}} </h2>
        {% endif %}

    </body>
</html>
